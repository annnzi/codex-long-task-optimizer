# CHANGELOG

## Unreleased

- Pending: release roadmap for v0.2

## v0.3.2 - 2026-08-21

- Markdown 输出同步展示应用名、版本号和输出协议，和 JSON 结果保持一致，便于直接贴入 Issue、PR 与智能体上下文。
- 新增 Markdown 元数据回归覆盖。
- 备份 manifest 新增触发时的项目签名，支持恢复审计时确认备份对应的精确项目状态。

## v0.3.1 - 2026-08-21

- 核心 CLI 时间戳改用明确的 UTC aware datetime，保持 `Z` 输出格式并消除 `datetime.utcnow()` 的弃用风险。
- 新增时间戳时区回归测试，避免报告和状态时间被误当成本地时间。
- PowerShell 定时链路贯通 `AllowSensitive` 显式放行开关；默认安全阻断不变，只有用户明确指定时才允许高风险文件进入自动提交。

## v0.3.0 - 2026-08-21

- JSON 计划结果新增 `schema_version`、`app` 和 `version` 元数据，便于智能体、Issue 流程和自动化流水线可靠识别输出契约。
- 新增自描述 JSON 输出回归测试，避免应用版本与报告版本脱节。
- 自动上传新增敏感文件安全闸门，默认阻止 `.env`、私钥、证书和凭据 JSON 等高风险路径进入公开提交。

## v0.2.4 - 2026-08-21

- 标签推送失败后记录 `pending_tag`，下一次启用标签上传时会先重试待推送标签，避免提交已上传但版本标签永久滞留本地。
- 新增待推送标签状态持久化回归覆盖。
- 动态状态 Markdown 回写复用原子文本写入，和状态 JSON 一样避免中断时产生截断文件。

## v0.2.3 - 2026-08-21

- 修复 commit 成功但 push 失败后的恢复路径：工作区干净但分支领先远端时，执行模式会重试推送本地提交。
- 新增未推送提交检测回归测试，避免上传失败后被误判为“无代码变更”。
- scheduler 锁改用 `CreateNew` 竞争并强制刷盘，降低并发接管和异常断电后的锁状态不一致风险；安装器提前校验 `MaxBackups`。

## v0.2.2 - 2026-08-21

- scheduler 现在显式检查 Python 子进程退出码，非零结果会向计划任务传播失败，避免“任务成功但维护失败”的假状态。
- 备份 ZIP 新增 `backup-manifest.json`，记录格式、创建时间、轮次版本标记和文件数量，便于恢复审计。

## v0.2.1 - 2026-08-21

- 状态 JSON 和 ZIP 备份改为临时文件写完后原子替换，降低定时任务中断留下半文件的风险。
- 新增状态保存回归测试，确保下一轮能读到完整状态且不遗留临时文件。
- 定时 PowerShell 封装层贯通 `MaxBackups` 参数，并让计划任务安装器也支持仓库 `.venv` / `venv` Python 回退，避免配置只在底层生效。

## v0.2.0 - 2026-08-21

- 定时上传入口新增仓库本地 `.venv` / `venv` Python 回退，降低 Windows 计划任务因系统未配置 Python 命令而无法启动的概率。
- 完成第 20 轮节点：每 10 轮大版本、每 2 轮小版本的维护节奏进入 `0.2.0`。
- 继续保持上传默认关闭，只有显式启用执行参数时才允许 commit、push 和标签推送。
- 修复 Windows 定时任务状态输出乱码，并新增版本推导回归测试，确保轮次不会再次叠加到项目版本。

## v0.1.8 - 2026-08-21

- 维护上传器新增备份保留上限，默认只保留最近 10 个备份，避免长期运营造成备份目录无限增长。
- 定时器 `bootstrap` 默认采用幂等更新策略，重复初始化时自动更新计划任务配置。
- 补齐本地第 18 轮维护的版本记录，保持 CLI、打包元数据和运维状态一致。
- 修复维护上传器重复递增版本号的问题：轮次仅用于上传窗口，备份和标签改用当前项目版本源，避免 `0.1.8` 被错误显示为 `0.1.16`。

- 优化版本演进算法：`maintenance_uploader` 的 `cycle_version` 改为基于累计周期计数递增，避免跨 100 轮后版本号与提交标签重复。
- 维护上传器新增 `--tag-on-upload`（配合 `--auto-execute` 可在到达窗口提交后自动创建并推送 `v<cycle_version>` 标签），提升每轮上传成果可追溯性，减少手工打 tag 漏项。
- 修复 `maintenance_uploader.py` 在 Python 3.9 兼容性问题（`str | None`/`Path | None` -> `Optional[...]`），避免在 3.9 环境跑定时脚本时报语法错误。
- 修复 `maintenance_uploader.py` 的版本号累计口径：`_derive_cycle_version` 改为基于累计总轮次 `cycles_completed * max_round + round_count` 递增，避免 `max_round` 与 `version-step` 不可整除时跨周期遗漏版本递增。
- 新增 `scripts/maintenance_uploader_cadence.ps1`：一键定时运维入口（init/status/run/run-loop/health/stop），内置 2 轮小版本、10 轮大版本节奏口径，默认 25/120 分钟窗口，支持 `-AutoCommit` 与健康日报输出，简化持续上传部署链路。
- 修复 `maintenance_uploader_cadence.ps1` 中参数数组尾随逗号导致的 PowerShell 解析失败（ParserError），恢复 `-Mode help` 与命令入口可用性。
- 优化 `maintenance_uploader.py` 备份命名策略：变更签名时生成的备份文件新增 `r<轮次>-v<版本>` 标记，便于“每次更新都有可追溯备份”与后续审计。
- 调整 `maintenance_uploader.py` 的 `auto-round` 触发：仅在文件签名发生变化时才推进轮次，避免无变更时误推进 `auto-round`。

- 维护脚本修复：`maintenance_uploader.py` 将 `--set-round` 的 `0` 明确为“重置到 1”，避免文档中“复位”操作跳到末轮；同时修正窗口计数写回时的轮次行覆盖逻辑，避免重复反引号副作用。
- 优化 `max_tokens` 切片在中文无空格长句场景下的行为：在无分词空格时按字符回退计数，补充中文边界回归测试。
- 补充复杂度评分在中文无空格场景下的 token 估算，统一复用计数逻辑，降低复杂度低估导致的计划风险误判。
- 修复阶段估算 `est_tokens` 的口径不一致：`Checkpoint` 估算改为复用 `_estimate_tokens`，并补充中文片段估算回归用例。
- 扩展分段分割边界：`_split_text` 增加中文逗号（，）、顿号（、）与英文逗号（,）识别，降低长句连贯段导致的超限风险。
- 提供 `--text` 与 `--input -` 两种免文件输入方式：直接文本和 stdin 可直接驱动解析，减少集成接入成本。
- 默认示例文件缺失时自动回退到内置示例文本，避免安装后首次运行即失败。
- 统一 Skill 文档与实际调用方式（移除未实现的 JSON 输入载荷约定，新增 `--text`/`--input -` 使用说明），降低外部智能体误调用概率。
- CI 增加 `--text`、stdin 与 `--status` 的 smoke 检查，接入后行为更可验证。
- 测试覆盖：新增输出目录自动创建回归（`--out` 嵌套目录可写）。
- 完善打包与发布链路：`pyproject.toml` 增加 `setuptools` package-find 配置（确保可安装场景可识别 `src` 包），CI 增加 console script 验证（`codex-long-task-optimizer --text` / `--status`）。
- 添加大众友好的安装入口：新增 `task-optimizer` 与 `agentic-task-optimizer` 控制台脚本别名（与 `codex-long-task-optimizer` 并存）。
- 优化输入标准化：`--text` 会先剥离首尾空白并保持 `--text` 优先于 `--input` 的解析策略；新增优先级回归测试，降低命令歧义风险。
- 强化路径健壮性：`--input` 与 `--out` 统一支持用户目录展开（`~`），并在输出写入失败时返回统一错误码，提升不同环境下接入成功率；新增 `~` 路径读取回归测试。
- 标准化 stdin 输入行为：`--input -` 将 stdin 文本先 `strip()` 后处理，与 `--text` 一致，减少空白污染；新增对应回归用例。
- 解析增强：兼容列表/编号前缀输入（如 `- 目标：...`、`1. GOAL: ...`），提升从任务清单粘贴时字段识别率；新增列表与数字前缀标签解析回归。
- 解析增强：兼容 Markdown 标题前缀输入（如 `## 目标：...`、`### GOAL: ...`），提升从文档/issue 直接粘贴时字段识别率；新增标题格式解析回归。
- 解析增强：兼容 Markdown 任务列表复选框前缀（如 `- [ ]`、`- [x]`）的字段标签，减少 copy/paste 任务时的识别漏报；新增复选框前缀解析回归。
- 解析增强：兼容组合前缀样式（如 `1. [x]`、`2) [ ]`、`+ [x]`）的标签行，覆盖更多粘贴场景下的任务清单格式。
- 解析增强：约束英文无冒号标签识别边界，防止 `goalpost` 等单词误触发 `goal/scope/...` 标签；补充边界回归测试，减少误归类风险。
- 解析增强：支持全角/英文方括号包裹标签格式（如 `【目标】...`、`(范围)...`、`[验收]...`）的识别，覆盖会议纪要/需求文档常见样式；新增相应回归测试。
- 解析增强：支持括号包裹标签的可选冒号写法（如 `【目标】：...`、`[范围] ...`），并补充对应回归测试。
- 解析增强：支持中文数字序号前缀（如 `1）`、`（2）`）的标签行，减少会议纪要中编号格式导致的字段识别偏移。

## v0.1.6 - 2026-08-17

- 建立五轮版本节奏：按“每5轮完整优化后升版本”执行，当前仓库维护目标对齐提交节奏。
- 优化版本一致性：将 CLI 运行版本来源统一为 `src.__version__`，避免版本散落。
- 增加版本常量测试覆盖，降低后续发布误差。
- 优化分片逻辑：支持无分隔符长文本按 max_tokens 精准切分，减少单阶段 token 超限。
- 优化复杂度口径：`complexity` 以 token 词数计算并修正文案。
- 增加 `--status` 状态快照，便于日常版本与运行环境追溯。
- 提升字段解析性能与稳定性：`SECTION_KEY_MAP` 常量化，增强英文标签兼容。
- 修正文档-实现不一致：统一 `README`/`docs/USAGE_FOR_AGENTS.md` JSON 输出契约与 CLI 实际字段。
- 增加空输入与输出 schema 回归测试，覆盖边界场景。
- 修复英文标签前缀解析边界：支持 `GOALS/OUTPUTS` 这类非完全匹配标签（如带复数后缀）仍能正确归类。
- 修复 `complexity` 评分函数里 `int.count` 异常并补充风险加成回归测试，降低运行时崩溃风险。
- 补强 `--no-risk`：Markdown 输出不再出现风险建议块和阶段风险列表，和参数语义对齐。
- 修复段落解析对“无冒号紧贴关键字”场景的文本截断：`目标完成...`、`范围覆盖...` 现在能正确识别并提取正文。

## v0.1.5 - 2026-08-17

- 重构 README 为中英双语首页（中文+英文并列段落）
- 保持 2 分钟接入、30 秒验收、命令清单的双语可读性
- 更新项目版本为 `0.1.5`

## v0.1.4 - 2026-08-17

- 添加 `SKILL.md`，定义可供其他智能体复用的能力边界与调用参数。
- 添加 `docs/USAGE_FOR_AGENTS.md`，提供智能体接入模板与失败处理建议。
- README 新增“技能接入”入口与双语可读说明。
- 更新项目版本标识为 `0.1.4`。

## v0.1.1 - 2026-08-17

- 增加 `--version`、`--out` 参数
- 优化字段解析：支持 `目标/scope` 等别名
- 改进切分参数校验，避免 0/负数导致异常行为
- 文档更新：README 与贡献流程补充运营节奏

## v0.1.0 - 2026-08-14

- 首次发布 MVP：任务文本分阶段输出（Markdown/JSON）
- 引入风险与回退建议
- 完成基础 pytest 用例

