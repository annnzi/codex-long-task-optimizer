[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-long-task-optimizer"
version = "0.1.0"
description = "Generate actionable checkpoints from long tasks for Codex-style execution."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Codex Project"}]

[project.urls]
Homepage = "https://github.com/<YOUR_GITHUB_USERNAME>/codex-long-task-optimizer"
